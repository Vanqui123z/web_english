{
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && cp -r frontend/dist/* backend/public/ && npm install --prefix backend && npm run build --prefix backend",
    "start": "node backend/dist/main.js"
  }
}
